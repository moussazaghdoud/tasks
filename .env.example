# Copy to .env.local and fill in to let Claude analyze voice memos.
# Without it, Hence falls back to on-device analysis.
# Create a key at https://platform.claude.com/settings/keys
ANTHROPIC_API_KEY=
