node_modules
dist
dist-server

# Signing material must never be committed — it belongs in GitHub secrets and
# in the private certificates repository.
*.p8
AuthKey_*
*.p12
*.cer
*.certSigningRequest
*.mobileprovision
fastlane/*.keychain*

# Native build output (the iOS project itself is committed)
build
ios/App/build
ios/App/App/public
fastlane/report.xml
fastlane/README.md
*.ipa
*.dSYM.zip
*.local
.env
.env.*
!.env.example
.DS_Store
