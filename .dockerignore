# Keep the build context small: everything here is either rebuilt inside the
# image, irrelevant to the server, or must never reach it.
node_modules
dist
dist-server
build

# The iOS app is built on a macOS runner, not here.
ios
fastlane
docs

.git
.github
*.log
.env
.env.*
*.p8
*.p12
*.mobileprovision
